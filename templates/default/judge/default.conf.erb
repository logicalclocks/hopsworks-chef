server {
    listen <%= node['judge']['port'] %>;

    location / {
        default_type application/json;
        return 200 '{"active": "<%= node['install']['regions']['primary'] %>", "secondary": "<%= node['install']['regions']['secondary'] %>"}';
    }
}